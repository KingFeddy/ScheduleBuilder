DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/njit_dev
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGc...
# Shared default on API and scraper services; the frontend reads /api/terms.
# Six ASCII digits ending in 10 (Spring), 50 (Summer), or 90 (Fall).
CURRENT_TERM=202690
# Leave unset on both services to use the shared expanded collection default.
# Optional override: use identical values on API and scraper services.
# Configuration does not prove collection; check /api/catalog/coverage.
# CATALOG_SUBJECTS=CS,MATH
# Broad elective browser subjects; this list does not establish GER eligibility.
GER_SUBJECTS=COM,ENG,HUM,HIST,PHIL,PSYC,SOC,STS,ARH,MUS,LIB,SSC
APP_ENV=development
CORS_ORIGINS=http://localhost:3000
SENTRY_DSN=
LOG_LEVEL=INFO
